(library
  (name tjr_fs_shared)
  (public_name tjr_fs_shared)
  (preprocess (pps ppx_deriving_yojson ppx_bin_prot))
  (flags (:standard "-w" "-39"))  ; -27 is unused var, -39 is unused rec flag
  (libraries 
    bin_prot ppx_bin_prot yojson ppx_deriving_yojson 
    tjr_lib tjr_monad))  

; we include monad so we don't have to explicitly depend on it in later packages

(include_subdirs unqualified)
